<?xml version="1.0" encoding="UTF-8"?>

<Shell
    x:Class="mandelbrot.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:mandelbrot.Views"
    Title="mandelbrot">
    
    <TabBar>
        <ShellContent Title="Render" ContentTemplate="{DataTemplate pages:RenderPage}" />
        <ShellContent Title="Settings" ContentTemplate="{DataTemplate pages:Settings}" />
    </TabBar>

</Shell>